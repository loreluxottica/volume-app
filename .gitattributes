# Normalize line endings: LF in the repo for everything.
# The app runs on Linux (Databricks Apps), so code must stay LF.
* text=auto eol=lf

# PowerShell scripts are Windows-only — keep CRLF on checkout.
*.ps1 text eol=crlf